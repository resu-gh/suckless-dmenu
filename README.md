### suckless - dmenu

```console
rule help                           print help
rule build                          @combo [clean deps compile]
rule deps                           install required dependencies
rule compile                        compile dmenu
rule clean                          uninstall dmenu
```
